
1-260107
- 根据原型图生成一个笔记类微信小程序，前端语言用最合适的，先实现前端交互页面，功能支持后期添加，按钮、标签、图片先用框架原生按钮标签，支持后期替换贴图，在当前工作空间下生成所有项目文件，包括Readme文件，要求目录分级合理； 十分钟生成44个文件
- 几个index.ts都在报错，每个ts都打开一下看看有没有报错； 直接告诉ai：检查所有ts，看还有没有报错； 检索14个ts，查出1个错误，修复；
- 打开微信开发工具，导入工程，使用测试号；
- 工程下npm install：added 2 packages in 5s；
- 微信开发工具-工具-构建npm：构建成功；
- 微信开发工具-依然报错：[ app.json 文件内容错误] app.json: 未找到 ["pages"][0] 对应的 pages/timeline/index.js 文件
-- 微信开发者工具**不支持直接编译 TypeScript**，需要先将 `.ts` 文件编译成 `.js` 文件。 
    **安装 TypeScript 编译器**  npm install --save-dev typescript
    **编译所有 TypeScript 文件**  tsc -- 会在每个 `.ts` 文件旁边生成对应的 `.js` 文件
